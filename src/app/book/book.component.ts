import { Component, Input } from '@angular/core';

@Component({
  moduleId: module.id,
  selector: 'book',
  templateUrl: './book.component.html'
})

export class BookComponent {
  constructor() { }
}